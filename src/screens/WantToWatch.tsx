import { Text } from "@gluestack-ui/themed";
import { ScrollView } from "react-native";

const WantToWatch = () => {
	return (
		<ScrollView style={{ flex: 1, backgroundColor: "#ffff" }}>
			<Text>WantToWatch</Text>
		</ScrollView>
	);
};

export default WantToWatch;
